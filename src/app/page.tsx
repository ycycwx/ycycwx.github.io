import {css} from '../../styled-system/css';

const Home = () => {
    return (
        <div className={css({fontSize: '2xl', fontWeight: 'bold'})}>
            Hello World
        </div>
    );
};

export default Home;
